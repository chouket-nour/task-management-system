apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-service
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicas.project }}
  selector:
    matchLabels:
      app: project-service
  template:
    metadata:
      labels:
        app: project-service
    spec:
      serviceAccountName: rfc-waiter
      {{- include "rfc.init.mongodb" . | nindent 6 }}
      containers:
        - name: project-service
          image: {{ .Values.image.registry }}/rfc-project-service:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 5004
          env:
            {{- include "rfc.env.common" . | nindent 12 }}
            - name: MONGO_URI
              value: {{ .Values.mongo.uriProject }}
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu }}
              memory: {{ .Values.resources.requests.memory }}
            limits:
              cpu: {{ .Values.resources.limits.cpu }}
              memory: {{ .Values.resources.limits.memory }}
          livenessProbe:
            httpGet:
              path: /health
              port: 5004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5004
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: project-service
  ports:
    - port: 5004
      targetPort: 5004